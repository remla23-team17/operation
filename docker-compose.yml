services:
  app:
    image: ghcr.io/remla23-team17/app
    ports:
      - "8081:8081"
    networks:
      network:
        ipv4_address: 172.20.0.3
    depends_on:
      - model-service

  model-service:
    image: ghcr.io/remla23-team17/model-service
    ports:
      - "8080:8080"
    networks:
      network:
        ipv4_address: 172.20.0.2
      

networks:
  network:
    driver: bridge
    ipam:
      config:
        - subnet: 172.20.0.0/16
          gateway: 172.20.0.1
